Le rapport se trouve dans "rapport.pdf"

Le code est dans 'exo1.c', 'exo2.c', 'exo3.c', 'customlibs/*.c'

Pour que les fonctions 'fork()' et 'kill()' compile sans warning,
j'ai du ajouter:

# define _DEFAULT_SOURCE

dans 'customlibs/lab.h'

Ca compile sans warning chez moi... en esperant que tout
se passe bien de votre coté

Les fichiers de tests sont ceux de la V2 du projet
et n'ont pas été modifié (à savoir):
	- Makefile
	- exec1.sh, exec2.sh, exec3.sh
	- dossier 'tests'
	- check_solution.py

Le dossier '.bkp' contient ma 1ère implementation, elle permet
d'illustrer mon rapport (comparatifs de performances).
MAIS c'est bien le code à la racine du dossier qui correspond à mon rendu final.

Romain PEREIRA
