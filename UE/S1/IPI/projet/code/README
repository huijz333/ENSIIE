Le rapport se trouve dans "rapport.pdf"

Le code est dans 'exo1.c', 'exo2.c', 'exo3.c', 'customlibs/*.c'

Pour que les fonctions 'fork()' et 'kill()' compile sans warning,
j'ai du ajouter:

# define _DEFAULT_SOURCE

dans 'customlibs/lab.h'

Ca compile sans warning chez moi... en esperant que tout
se passe bien de votre coté

Pour tester le projet, il suffit de déplacer les fichiers
de la V2 du projet à la racine de ce dossier.
	- Makefile
	- exec1.sh, exec2.sh, exec3.sh
	- check_solution.py
	- tests

Mon rendu final correspond à la racine de ce dossier : le dossier '.bkp' contient ma 1ère
implementation, elle permet d'illustrer mon rapport (comparatifs de performances).

Romain PEREIRA
